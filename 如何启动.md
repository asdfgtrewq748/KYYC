# 快速启动指南

## ⚠️ 首次使用必读

### 第一步：安装PyTorch（仅首次需要）

如果遇到 `ModuleNotFoundError: No module named 'torch'` 错误，请先安装PyTorch：

**在Anaconda Prompt中运行**：
```bash
conda activate kyyc_py311
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
```

> 注意：下载约2-3GB，需要几分钟时间。安装完成后关闭并重新打开Streamlit。

---

## 方法1: 使用Anaconda Prompt（最推荐）⭐

1. 打开 **Anaconda Prompt**（开始菜单搜索）
2. 运行以下命令：
```bash
cd d:\xiangmu\KYYC
conda activate kyyc_py311
streamlit run STGCN.py
```

## 方法2: 在项目目录打开PowerShell

1. 在文件资源管理器中打开文件夹：`d:\xiangmu\KYYC`
2. 在地址栏输入 `powershell` 并回车
3. 运行：
```powershell
conda activate kyyc_py311
streamlit run STGCN.py
```

## 方法3: 双击bat脚本

双击 `启动训练界面.bat` 文件

如果提示conda未找到，请使用方法1。

## 方法4: 右键运行PowerShell脚本

1. 右键点击 `启动训练界面.ps1`
2. 选择"使用PowerShell运行"

---

## 常见问题

**Q: 提示"ModuleNotFoundError: No module named 'torch'"**
A: PyTorch未安装。运行：
```bash
conda activate kyyc_py311
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
```

**Q: 提示"File does not exist: STGCN.py"**
A: 您不在项目目录。请先运行 `cd d:\xiangmu\KYYC`

**Q: conda命令未找到**
A: 请使用Anaconda Prompt，不要使用普通PowerShell

**Q: 环境激活失败**
A: 确认环境名称为"kyyc_py311"，运行 `conda env list` 查看

**Q: 显存不足 (CUDA out of memory)**
A: 启动后在界面中减小batch_size（64→32→16）

---

启动成功后，浏览器会自动打开 http://localhost:8501

## 🎯 完整依赖安装（如有需要）

如果缺少其他库，运行：
```bash
conda activate kyyc_py311
pip install streamlit pandas scipy numpy matplotlib
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
```
